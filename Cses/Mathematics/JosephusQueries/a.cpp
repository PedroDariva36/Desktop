#include <bits/stdc++.h>

using namespace std;

int main (int argc, char *argv[]){
    int t;
    long n,k;
    cin.tie(0)->sync_with_stdio(0);
    cin >> t;
    while (t--) {
        cin >> n >>  k;
        
        if(k%2==0){
            cout << k/2 << endl;
        }
        


    }

    return 0;
}
